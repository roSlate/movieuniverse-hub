## On the stack chosen


### Spring Boot + React


### H2 file mode

It doesn't really need an external service so the app can start quickly with one command on a clean machine.
File mode can handle persistence across restarts, even though it is not suitable for production. It is a good choice for 
development and testing.